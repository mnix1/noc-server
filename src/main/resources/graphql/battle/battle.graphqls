enum BattleStatus {
    SEARCHING_OPPONENT
    PREPARING
    IN_PROGRESS
    FINISHED
}

enum BattleResult {
    BATTLE_SIDE_0_WON
    BATTLE_SIDE_1_WON
    DRAW
    EMPTY
}

enum BattleSide {
    BATTLE_SIDE_0
    BATTLE_SIDE_1
}